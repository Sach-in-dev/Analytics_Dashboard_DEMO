import SearchesTemplate from "@/modules/searches/templates"


export const metadata = {
  title: "Search Analytics",
  description: "Analyze overall search behavior, including top keywords and zero-result queries, to improve discovery. Uncover conversion patterns from search results to connect users with the products they want faster.",
}

export default function Page() {
    return <SearchesTemplate />
}
