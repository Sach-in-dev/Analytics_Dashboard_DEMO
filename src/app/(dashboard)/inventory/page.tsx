import InventoryPage from "@/modules/inventory/section/inventory-page"

export const metadata = {
  title: "Inventory Metrics",
  description: "Monitor your inventory levels to prevent stockouts and minimize overstocking. Streamline your supply chain operations and ensure you always have the right products available to meet customer demand.",
}

export default function Page() {
  return <InventoryPage />
}
