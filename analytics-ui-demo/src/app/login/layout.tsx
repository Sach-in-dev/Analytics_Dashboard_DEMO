export const metadata = {
  title: "Login",
  description: "Sign in to BeautyBarn Analytics Dashboard to access your e-commerce metrics and reports.",
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
